#include <iostream>
#include <map>
#include <vector>

int main()
{
	std::map<std::string, std:vector<std::string>> bs;
	int n;
	std::cin >> n;
	for(int i=0; i<n; ++i)
	{
		std::string query;
		std::cin >> query;
		if(query=="NEW_BUS")
		{
			std::string bus;
			int n_stops;
			std::cin >> bus >> n_stops;
			for()
		}
	}
}
